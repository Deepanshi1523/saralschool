'use strict';

/**
 * scholarship-enquiry router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::scholarship-enquiry.scholarship-enquiry');
